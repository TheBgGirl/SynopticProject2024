package com.example.farmsimulator.opengl

class Plane(width: Int, height: Int) {
    init {
        val vertices = FloatArray(width * height * 3 * 4)
        var i = 0
        while(i < vertices.size)
        {
            i++
        }
    }

}
